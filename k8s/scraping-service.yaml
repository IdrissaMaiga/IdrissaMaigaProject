apiVersion: v1
kind: Service
metadata:
  name: scraping-service
  namespace: product-assistant
  labels:
    app: scraping-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: scraping-service





