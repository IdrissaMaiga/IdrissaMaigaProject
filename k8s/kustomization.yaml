apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: product-assistant

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - pvc.yaml
  - api-deployment.yaml
  - api-service.yaml
  - scraping-deployment.yaml
  - scraping-service.yaml
  - ai-deployment.yaml
  - ai-service.yaml
  - ingress.yaml
  - hpa.yaml

# Note: commonLabels removed to preserve service selectors
# Labels are defined directly in each resource file

